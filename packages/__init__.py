"""First-party packages for the S3M ElectricalTwin platform (Work Package 0).

This namespace groups the domain libraries that are shared across services:

* :mod:`packages.canonical_electrical_model` — the canonical electrical asset
  graph, energization state and the platform safety posture.
* :mod:`packages.s3m_engine_contract` — the contract between the S3M reasoning
  engine and the operator: packets in, recommendation cards out, plus the
  deterministic grounding gate, determinism guarantees and the audit chain.

Nothing in this namespace performs an LLM invocation. Work Package 0 defines the
contract, the guardrails and the vocabularies only.
"""
"""Canonical top-level namespace for S3M ElectricalTwin packages."""
"""Namespace package root for S3M ElectricalTwin engineering packages."""
"""Namespace package root for S3M ElectricalTwin canonical packages."""
"""S3M ElectricalTwin package namespace."""
